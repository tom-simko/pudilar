---
import { legalDetails } from '../content/site';

interface Props {
  labels: {
    registeredOffice: string;
    country: string;
    companyId: string;
    taxId: string;
    vatId: string;
    register: string;
    registerName: string;
    section: string;
    insertNumber: string;
    established: string;
    establishedDate: string;
  };
  compact?: boolean;
}

const { labels, compact = false } = Astro.props;
---

<dl class:list={['company-details', { 'company-details--compact': compact }]}>
  <div>
    <dt>{labels.registeredOffice}</dt>
    <dd>
      {legalDetails.street}<br />
      {legalDetails.locality}<br />
      {labels.country}
    </dd>
  </div>
  <div>
    <dt>{labels.companyId}</dt>
    <dd>{legalDetails.companyId}</dd>
  </div>
  <div>
    <dt>{labels.taxId}</dt>
    <dd>{legalDetails.taxId}</dd>
  </div>
  <div>
    <dt>{labels.vatId}</dt>
    <dd>{legalDetails.vatId}</dd>
  </div>
  <div>
    <dt>{labels.register}</dt>
    <dd>
      {labels.registerName}<br />
      {labels.section}: {legalDetails.registerSection}<br />
      {labels.insertNumber}: {legalDetails.registerInsert}
    </dd>
  </div>
  <div>
    <dt>{labels.established}</dt>
    <dd>{labels.establishedDate}</dd>
  </div>
</dl>
